export type Feature = {
  image_url: string;
  title: string;
  description: string;
};

export type Service = {
  image_url: string;
  title: string;
  description: string;
};

export type Testimonial = {
  image_url: string;
  name: string;
  job_description: string;
  testimonial: string;
};
